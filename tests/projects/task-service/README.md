# Task service

The task service creates tasks and records their completion. Applications can
provide another repository implementation without changing the service API.

See the [task service reference](docs/reference/task-service.md) for source-bound
parameter, return-type, and error documentation.
